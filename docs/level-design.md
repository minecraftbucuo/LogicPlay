# LogicPlay 关卡设计

这个文件用于逐关设计正式关卡。当前按确认顺序逐关追加。

## Level 1：基础移动

- 状态：✅ 已实现
- 关卡目标：让玩家学习最基础的移动。
- 教学重点：使用 `robot.move_forward()` 控制机器人向前移动。
- 通关目标：移动到终点即可通关，并解锁下一关。
- 地图尺寸：5x5。
- 起点：`{ x: 0, y: 2, direction: 'right' }`
- 终点：`{ x: 4, y: 2 }`
- 障碍物：无。
- 收集品：无。
- 特殊机制：进入第一关时先显示一段故事背景，居中展示在屏幕中间；玩家点击确认后再开始游戏。
- 通关条件：机器人到达终点格。
- 初始代码：只提供最基础的一行移动提示，不给出完整答案。
- 标准答案：通过游戏界面“显示标准答案”按钮查看。
- 提示说明：通过游戏界面“提示说明”按钮查看过关思路和当前关卡命令/API 用法。

```python
# 提示：机器人需要多次向前移动
robot.move_forward()
# 继续补充代码，让机器人到达终点
```

- 设计说明：这一关只让玩家建立“写一行代码，机器人移动一步”的直觉，不引入转向、障碍物或其他机制。

### 第一关开场故事

- 展示时机：玩家进入第一关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击“开始任务”后关闭弹窗，再进入可操作状态。
- 故事文案：欢迎来到 LogicPlay。你的机器人已经在训练场待命，但它还不会自己行动。现在，请写下最基础的移动指令，让机器人一步一步抵达绿色终点。

### 第一关实现任务

- ✅ 为关卡数据增加开场故事字段。
- ✅ 第一关进入时读取故事字段并展示居中弹窗。
- ✅ 弹窗关闭前禁止执行代码或操作关卡。
- ✅ 点击“开始任务”后关闭弹窗并开始游戏。

## Level 2：绕过障碍

- 状态：✅ 已实现
- 关卡目标：让玩家学会通过转向绕过障碍，到达终点。
- 教学重点：使用 `robot.turn_left()` 和 `robot.turn_right()` 改变机器人朝向。
- 通关目标：绕过中间的障碍物，移动到绿色终点后通关并解锁下一关。
- 地图尺寸：5x5。
- 起点：`{ x: 0, y: 2, direction: 'right' }`
- 终点：`{ x: 4, y: 2 }`
- 障碍物：中间竖向墙体：`{ x: 2, y: 1 }`、`{ x: 2, y: 2 }`、`{ x: 2, y: 3 }`
- 收集品：无。
- 特殊机制：进入第二关时先显示一段故事背景，居中展示在屏幕中间；玩家点击确认后再开始游戏。
- 通关条件：机器人绕过障碍并到达终点格。
- 初始代码：只提供转向命令示例，不给出完整绕路答案。
- 标准答案：通过游戏界面“显示标准答案”按钮查看。
- 提示说明：通过游戏界面“提示说明”按钮查看过关思路和当前关卡命令/API 用法。

```python
# 提示：转向只会改变方向，不会前进
robot.move_forward()
robot.turn_right()
# 继续绕过障碍，到达终点
```

- 设计说明：这一关承接第一关的直线移动，在地图中加入障碍物，迫使玩家使用左右转向规划路径。

### 第二关开场故事

- 展示时机：玩家进入第二关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击“开始任务”后关闭弹窗，再进入可操作状态。
- 故事文案：训练场升起了第一道障碍墙。直线前进已经不够了，你需要让机器人学会转向：`robot.turn_left()` 可以向左转，`robot.turn_right()` 可以向右转。转向只会改变方向，不会前进。绕过障碍，抵达终点。

### 第二关实现任务

- ✅ 将现有“绕过障碍”关卡作为正式第二关。
- ✅ 为第二关增加开场故事字段。
- ✅ 在故事中明确提示 `robot.turn_left()` 和 `robot.turn_right()` 的用途。
- ✅ 调整初始代码，让玩家有示例但仍需要完成剩余路径。

## Level 3：重复前进

- 状态：✅ 已实现
- 关卡目标：让玩家理解重复指令可以用 `for` 循环简化。
- 教学重点：使用 Python 的 `for` 循环重复执行 `robot.move_forward()`，并在提示说明中补充 `while` 循环的基本用法。
- 通关目标：在更大的训练场中移动到终点，通关后解锁下一关。
- 地图尺寸：12x12。
- 起点：`{ x: 0, y: 6, direction: 'right' }`
- 终点：`{ x: 11, y: 6 }`
- 障碍物：无。
- 收集品：无。
- 特殊机制：进入第三关时先显示一段故事背景，居中展示在屏幕中间；玩家点击确认后再开始游戏。
- 通关条件：机器人到达终点格。
- 初始代码：只提供 `for` 循环结构提示，不给出正确循环次数。
- 标准答案：通过游戏界面“显示标准答案”按钮查看。
- 提示说明：通过游戏界面“提示说明”按钮查看过关思路和当前关卡命令/API 用法。

```python
# 提示：用 for 循环重复执行前进
for i in range(3):
    robot.move_forward()
# 修改循环次数，让机器人走到终点
```

- 设计说明：这一关把网格扩大到 12x12，终点距离更远。如果继续逐行写 `robot.move_forward()` 会显得重复，因此自然引出 `for` 循环。提示说明中同时补充 `while` 循环：它适合“不确定要重复多少次、需要根据条件继续”的场景，但本关距离固定，所以 `for` 更直观。

### 第三关开场故事

- 展示时机：玩家进入第三关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击“开始任务”后关闭弹窗，再进入可操作状态。
- 故事文案：训练场被扩展到了更远的距离。你当然可以一行一行写很多次 `robot.move_forward()`，但程序员会用更聪明的方式：`for` 循环可以让同一条指令重复执行。试着用循环，让机器人抵达远处的终点。

### 第三关实现任务

- ✅ 创建第三关关卡文件。
- ✅ 设置 12x12 地图，让距离变长以引出循环。
- ✅ 为第三关增加开场故事字段，提示 `for` 循环的用途。
- ✅ 将第三关接入关卡列表。

## Level 4：判断前方

- 状态：✅ 已实现
- 关卡目标：让玩家开始学习 `if` 条件分支语句。
- 教学重点：根据 `robot.sense()` 的结果判断前方是否可走，而不是写死固定路线。
- 新增 API：`robot.sense()`。
- API 设计：`robot.sense()` 返回机器人前方一格的内容，例如 `wall`、`empty`、`target`。
- 新增要素：`?` 神秘方块。
- 神秘方块规则：它看起来永远是 `?`，但实际可能是墙，也可能是空气。
- 随机规则：第四关中一整列都是 `?` 神秘方块，每次进入或重置关卡时，随机且仅随机一个神秘方块是空气，其余全部是墙。
- 通关目标：机器人必须用条件判断找到唯一通路，穿过神秘方块列并到达终点。
- 地图尺寸：7x7。
- 起点：`{ x: 0, y: 0, direction: 'right' }`
- 终点：`{ x: 6, y: 6 }`，保持固定，避免玩家通过终点位置反推空气格。
- 神秘方块列：`x = 3`，`y = 0..6`。
- 收集品：无。
- 特殊机制：进入第四关时先显示一段故事背景，居中展示在屏幕中间；玩家点击确认后再开始游戏。
- 通关条件：机器人穿过本局唯一空气神秘方块，并到达对应终点格。
- 初始代码：只提供 `if` 判断结构提示，不给出完整扫描神秘列的答案。
- 标准答案：通过游戏界面“显示标准答案”按钮查看。
- 提示说明：通过游戏界面“提示说明”按钮查看过关思路、`robot.sense()` 返回值和当前关卡命令/API 用法。

```python
# 提示：先走到神秘方块列前
for i in range(2):
    robot.move_forward()

# 提示：用 if 判断前方是不是墙
if robot.sense() == 'wall':
    robot.turn_right()
else:
    robot.move_forward()
```

- 设计说明：这一关刻意防止玩家用固定路线通关。因为唯一空气格每次都会变，玩家只有使用 `robot.sense()` 和 `if` 判断，才能稳定找到通路。

### 第四关开场故事

- 展示时机：玩家进入第四关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击“开始任务”后关闭弹窗，再进入可操作状态。
- 故事文案：训练场出现了一列神秘方块：它们看起来都一样，但每次任务开始时，只有一个方块其实是空气，其它都是墙。固定路线已经不可靠了，你需要用 `robot.sense()` 判断前方，再用 `if` 决定是否前进。

### 第四关待实现任务

- ✅ 为机器人增加 `robot.sense()` 感知 API。
- ✅ 设计 `sense()` 的返回值规则，至少支持 `wall`、`empty`、`target`。
- ✅ 增加 `?` 神秘方块关卡要素。
- ✅ 实现一整列神秘方块，且每局随机一个为空气。
- ✅ 创建第四关关卡文件。
- ✅ 为第四关增加开场故事字段，提示 `if` 的用途。
- ✅ 将完整解法放入标准答案按钮，避免初始代码直接给出答案。

## Level 5：封装动作

- 状态：✅ 已实现
- 关卡目标：让玩家学会使用函数，把“绕过一组门并收集星星”的动作封装起来并多次调用。
- 教学重点：使用 Python 的 `def` 定义函数，用函数名加括号调用函数。
- 设计动机：前几关已经学习了移动、转向、循环和条件判断。这一关引入“把一组指令命名并复用”的思想，让玩家理解函数不是为了少写一行代码，而是为了让重复路线更清晰、更容易维护。
- 通关目标：机器人依次绕过三组相同结构的障碍门，收集每个门后的星星，并抵达终点。
- 地图尺寸：12x12。
- 起点：`{ x: 0, y: 8, direction: 'right' }`
- 终点：`{ x: 11, y: 8 }`
- 障碍物：三组相同的“门形障碍”，每组都会挡住直线路径，必须执行同一套绕行动作。
- 障碍布局：
  - 第一组门：`x = 2`，墙体位于 `{ x: 2, y: 7 }`、`{ x: 2, y: 8 }`、`{ x: 2, y: 9 }`。
  - 第二组门：`x = 5`，墙体位于 `{ x: 5, y: 7 }`、`{ x: 5, y: 8 }`、`{ x: 5, y: 9 }`。
  - 第三组门：`x = 8`，墙体位于 `{ x: 8, y: 7 }`、`{ x: 8, y: 8 }`、`{ x: 8, y: 9 }`。
- 可通行路线：每组门上方留出绕行通道，玩家需要重复执行“上绕、横穿、下回原线并收集中间墙后的星星”的动作。
- 收集品：三颗星星，分别位于三组墙体中间墙的正后方：`{ x: 3, y: 8 }`、`{ x: 6, y: 8 }`、`{ x: 9, y: 8 }`。
- 特殊机制：进入第五关时先显示一段故事背景，居中展示在屏幕中间；玩家点击确认后再开始游戏。
- 通关条件：机器人收集全部三颗星星，并到达终点格。
- 初始代码：只提供函数定义和调用结构提示，不给出完整绕行动作。
- 标准答案：通过游戏界面“显示标准答案”按钮查看。
- 提示说明：通过游戏界面“提示说明”按钮查看过关思路、函数定义/调用方式和当前关卡命令/API 用法。

```python
# 提示：把“绕过一组门并拿到星星”的动作封装成函数
def bypass_gate():
    robot.turn_left()
    # 在这里补完整绕行动作

# 每组门后都有星星，重复调用函数会更清晰
bypass_gate()
bypass_gate()
bypass_gate()
```

- 设计说明：这一关通过“每组门后都有必须收集的星星”来制造真实重复需求。玩家需要三次完成同一套“绕门并拿星星”的动作，函数不再是生硬限制，而是最自然的表达方式：把这套动作命名为 `bypass_gate()`，每遇到一组门就调用一次。
- 防止误解：文案里要明确“定义函数不会自动执行，必须调用函数才会执行函数体”。
- 进阶提示：函数体内部仍然可以使用已经学过的 `for` 循环，例如用循环写连续前进。

### 第五关开场故事

- 展示时机：玩家进入第五关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击“开始任务”后关闭弹窗，再进入可操作状态。
- 故事文案：训练场出现了三组一模一样的障碍门，每个门后方都藏着一颗星星。只有收集全部星星并抵达终点，任务才算完成。把绕过一组门并拿到星星的动作封装成 `bypass_gate()`，然后在每组障碍前重复调用它。

### 第五关待实现任务

- ✅ 创建第五关关卡文件。
- ✅ 设计 12x12 地图和三组重复障碍门。
- ✅ 为第五关增加开场故事字段，提示函数的用途。
- ✅ 为第五关增加提示说明，解释 `def 函数名():`、函数调用和缩进规则。
- ✅ 在每组障碍门后的必经路线放置星星，并要求全部收集后才能通关。
- ✅ 将第五关接入关卡列表。
- ✅ 为第五关提供非完整初始代码和标准答案。

## Level 6：迷宫策略

- 状态：✅ 已实现
- 章节定位：第二章开篇，从“语法学习”进入“策略与算法思维”。
- 关卡目标：让玩家写出能适应多张地图的通用迷宫策略，而不是为单张地图硬编码路线。
- 教学重点：综合使用 `while` 循环、`if/else` 条件判断、`robot.sense()` 感知和函数封装。
- 核心策略：贴墙走。推荐使用右手法则：优先尝试右转，如果右侧可走就右转并前进；否则如果前方可走就前进；否则左转。只要迷宫结构满足“墙体连通、终点在边界可达路径上”，贴墙策略就能到达终点。
- 通关目标：同一份代码依次通过多组迷宫测试用例，所有测试用例都到达终点才算通关。
- 地图尺寸：9x9。
- 起点/终点：每个测试用例可不同，但都保证起点方向与推荐贴墙策略匹配。
- 障碍物：墙体明显增多，形成单通道或少分支迷宫。
- 收集品：无。
- 特殊机制：多测试用例。玩家点击执行后，系统使用同一份代码依次加载每个测试用例，每个测试用例都从自己的起点、墙体和终点开始执行。
- 通关条件：所有测试用例均到达各自终点。
- 失败条件：任意测试用例撞墙、执行超出循环预算、指令执行完仍未到终点，则本关失败，并显示失败的是第几个测试用例。
- 初始代码：提供 `while` 循环和贴墙走函数结构，但不直接给完整判断逻辑。
- 标准答案：通过游戏界面“显示标准答案”按钮查看。
- 提示说明：通过游戏界面“提示说明”按钮查看贴墙走策略、`robot.sense()` 返回值、循环退出条件和多测试用例规则。

```python
# 提示：同一份代码会在多张迷宫里测试
# 可以把“右侧是否可走”的判断封装成函数

def follow_wall():
    # 在这里写贴墙走策略
    pass

while robot.sense() != 'target':
    follow_wall()
```

- 设计说明：这一关不应该只考“能不能写一串路线”，而是考“能不能写一个策略”。多测试用例会让固定路线失效；只要玩家写的是贴墙走策略，就能适应不同迷宫布局。
- 策略说明：为了支持右手法则，玩家需要判断右侧和前方。当前 API 只有 `robot.sense()` 能看前方，因此标准答案可以通过“临时右转感知，再转回原方向”的方式判断右侧：
  - 右转。
  - 如果前方不是 `wall`，说明原本右侧可走，直接前进。
  - 如果前方是 `wall`，左转回原方向，再判断前方是否可走。
  - 前方也不可走时，左转继续寻找方向。
- 循环退出：可以使用 `while robot.sense() != 'target':` 作为核心循环，但需要注意：当目标不在正前方而机器人已经站到目标格时，`sense()` 不会返回 `target`。因此实现时更推荐由引擎在每步后检测是否到达终点，玩家代码可以使用固定步数上限或 `for` 安全循环防止无限循环。
- 推荐标准答案思路：使用 `for step in range(200):` 包裹贴墙策略，避免玩家代码自己写死循环；引擎在机器人到达终点后判定当前测试用例通过。

### 第六关测试用例设计

- 测试用例数量：第一版建议 3 组。
- 每组测试用例都必须满足：
  - 起点到终点一定可达。
  - 使用同一种贴墙策略一定能到达终点。
  - 不能存在会让贴墙策略绕入无限环的孤立墙岛。
  - 起点朝向应保证玩家使用的右手法则能稳定贴到主墙。
- 测试用例 A：入门迷宫
  - 目的：让玩家第一次看到“不是固定路线也能走出去”。
  - 特点：分支少，路径较短，右手法则很快到终点。
- 测试用例 B：转弯迷宫
  - 目的：验证玩家代码能处理连续转弯和死胡同。
  - 特点：包含多个凹角，必须多次右转/左转切换。
- 测试用例 C：长走廊迷宫
  - 目的：验证玩家代码不是只适配前两个小图。
  - 特点：路径更长，包含一段长走廊和回折。

### 第六关需要新增的引擎能力

- ✅ `LevelData` 支持 `testCases` 字段，每个用例包含 `start`、`target`、`walls`，必要时也可以包含 `collectibles` 等扩展字段。
- ✅ `GamePlay` 执行时检测当前关卡是否有 `testCases`。
- ✅ 如果有关卡测试用例，则同一份代码按顺序对每个测试用例执行。
- ✅ 每个测试用例开始前重置机器人、运行时状态和指令队列。
- ✅ 输出区显示测试进度，例如 `测试 1/3 通过`、`测试 2/3 撞墙失败`。
- ✅ 地图区域在测试执行时切换到当前测试用例地图。
- ✅ 所有测试用例通过后才调用 `completeLevel(level.id)`。

### 第六关开场故事

- 展示时机：玩家进入第六关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击“开始任务”后关闭弹窗，再进入可操作状态。
- 故事文案：训练场不再只有一张地图。接下来，机器人会面对多张不同迷宫；写死路线已经彻底失效。你需要写出一套通用策略：观察前方、决定转向、沿着墙走。只要策略正确，换一张迷宫也能走到终点。

### 第六关待实现任务

- ✅ 设计 `testCases` 数据结构。
- ✅ 实现多测试用例执行流程。
- ✅ 创建第六关迷宫关卡文件。
- ✅ 设计 3 组保证可达的贴墙迷宫测试用例。
- ✅ 为第六关增加开场故事字段。
- ✅ 为第六关增加提示说明，解释贴墙走策略。
- ✅ 将第六关接入关卡列表。
- ✅ 提供非完整初始代码和标准答案。

## Level 7：随机猎手

- 状态：📝 设计中
- 章节定位：策略思维进阶，从"适应固定地图"到"应对随机变化"。
- 关卡目标：让玩家设计一种系统性搜索策略，保证无论星星随机出现在地图哪个位置，机器人都能找到并吃掉它。
- 教学重点：理解"穷举搜索"思想——用蛇形遍历（zigzag）等方式覆盖整个地图，确保不遗漏任何格子。综合使用 `while` 循环、`for` 循环、`if/else` 条件判断、`robot.sense()` 和函数封装。
- 核心策略：蛇形扫描。逐行遍历地图：沿当前方向前进直到撞墙，然后转向到下一行，反向前进，如此反复，像贪吃蛇一样覆盖整个地图。
- 通关目标：机器人收集 3 颗随机出现的星星。
- 地图尺寸：5x5。
- 起点：`{ x: 0, y: 0, direction: 'right' }`
- 障碍物：无（空旷地图，方便蛇形遍历）。
- 收集品：星星，共需收集 3 颗。
- 星星规则：
  - 地图上同时只有 1 颗星星。
  - 星星初始位置随机生成在一个空白格子上。
  - 机器人吃到星星后，下一颗星星立即随机出现在另一个空白格子上。
  - 星星不会出现在机器人占据的格子上。
  - 吃完 3 颗星星即通关。
- 特殊机制：
  - 随机性：每次进入或重置关卡时，星星位置都会重新随机生成。同一份代码必须能在任意随机种子下通关。
  - 多轮执行：玩家代码需要在吃完一颗星星后继续搜索下一颗，直到收集满 3 颗。
  - 步数上限：引擎设置最大执行步数（如 300 步），防止无限循环。
- 失败条件：撞墙、执行超出步数上限、或指令执行完仍未收集满 3 颗星星。
- 初始代码：只提供基本结构和提示，不给出完整搜索策略。
- 标准答案：通过游戏界面"显示标准答案"按钮查看。
- 提示说明：通过游戏界面"提示说明"按钮查看过关思路、蛇形扫描策略和当前关卡命令/API 用法。

```python
# 提示：星星的位置是随机的，固定路线无法保证吃到
# 思考：怎样走才能覆盖地图上的每一个格子？

def scan_row():
    # 沿当前方向前进，走完一行
    pass

def next_row():
    # 转向到下一行，准备反向扫描
    pass

# 用循环重复扫描，直到收集满 3 颗星星
```

- 设计说明：前几关的搜索策略是"适应不同地图"，这一关的挑战升级为"适应同一地图上的随机变化"。玩家不能依赖任何固定位置信息，必须写出一种无论星星在哪都能覆盖到的系统性搜索方案。蛇形遍历（zigzag）是最直觉的解法：逐行扫描，到行末换行反向，像打印机一样覆盖整个地图。这一关也自然引出"算法正确性"的思考——怎样证明你的策略一定能找到目标？

### 第七关开场故事

- 展示时机：玩家进入第七关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击"开始任务"后关闭弹窗，再进入可操作状态。
- 故事文案：训练场开始投放随机补给——星星会随机出现在地图的某个角落，吃完一颗，下一颗又会在别处出现。固定路线彻底没用了。你需要让机器人掌握一种"不遗漏任何角落"的搜索方式，像扫描仪一样系统地覆盖整张地图。只要策略正确，星星无论出现在哪，都逃不过你的机器人。

### 第七关新增 API

- 保持简单，不新增 API，只用现有的 `robot.sense()` 返回 `'star'` 当机器人走到星星所在格子时。

### 第七关测试用例设计

- 测试用例数量：第一版建议 3 组（不同随机种子）。
- 每组测试用例：
  - 同一张 5x5 空旷地图。
  - 起点固定为 `{ x: 0, y: 0, direction: 'right' }`。
  - 星星位置由不同随机种子决定，保证 3 颗星星位置各不相同。
  - 玩家同一份代码必须在所有种子下都能收集满 3 颗星星。
- 通关条件：所有测试用例均收集满 3 颗星星。

### 第七关待实现任务

- ✅ 设计星星随机生成与收集机制。
- ✅ 实现星星吃完后自动在随机空白格重新生成。
- ✅ 实现收集计数器，收集满 3 颗判定通关。
- ✅ 创建第七关关卡文件。
- ✅ 为第七关增加开场故事字段。
- ✅ 为第七关增加提示说明，解释蛇形扫描策略。
- ✅ 将第七关接入关卡列表。
- ✅ 提供非完整初始代码和标准答案。

## Level 8：贪吃蛇

- 状态：✅ 已完成
- 章节定位：策略思维进阶，从"搜索随机目标"到"规划自避路径"。
- 关卡目标：让玩家设计一条覆盖整个地图的循环路径，在贪吃蛇玩法下保证蛇头不会撞到自己的身体，最终吃满 35 颗星星占满整个地图。
- 教学重点：理解"哈密顿回路"思想——找到一条经过每个格子恰好一次并回到起点的闭合路径，沿着它走就能保证蛇头永远有路可走，不会撞到自己。综合使用 `for` 循环、`while` 循环、函数封装。
- 核心策略：螺旋循环路径。从起点出发，先向下走到底，再向右走到墙，然后向上走一格，向左走到第二格（留出回程通道），再向上……如此螺旋向内收缩，最终回到起点形成闭合循环。沿着这个循环不断重复，蛇头始终走在蛇尾前面，永远不会撞到自己。
- 通关目标：机器人（蛇）收集 35 颗星星，蛇身长度达到 36（占满整个 6x6 地图）。
- 地图尺寸：6x6。
- 起点：`{ x: 1, y: 1, direction: 'down' }`（1-based 坐标，即 0-based 的 (0,0)）。
- 障碍物：无（空旷地图，蛇身本身就是障碍）。
- 收集品：星星，共需收集 35 颗。
- 星星规则：
  - 地图上同时只有 1 颗星星。
  - 星星初始位置随机生成在一个空白格子上。
  - 蛇头吃到星星后，蛇身长度增加一格，下一颗星星立即随机出现在另一个空白格子上。
  - 星星不会出现在蛇身占据的格子上。
  - 吃完 35 颗星星即通关（蛇身占满整个地图）。
- 贪吃蛇机制：
  - 蛇身长度：初始为 1（只有蛇头），每吃一颗星星增加 1 格。
  - 蛇身跟随：蛇头移动时，蛇身各节跟随前一节移动。
  - 碰撞判定：蛇头撞到地图边界或蛇身（除蛇尾外）即失败。
  - 蛇尾让位：蛇头移动到新格子时，蛇尾同时前移，因此蛇头可以移动到原蛇尾位置（不会撞自己）。
- 特殊机制：
  - 随机性：每次进入或重置关卡时，星星位置都会重新随机生成。同一份代码必须能在任意随机种子下通关。
  - 步数上限：引擎设置最大执行步数（如 500 步），防止无限循环。
- 失败条件：蛇头撞墙、蛇头撞到蛇身、执行超出步数上限、或指令执行完仍未收集满 35 颗星星。
- 初始代码：只提供基本结构和提示，不给出完整循环路径。
- 标准答案：通过游戏界面"显示标准答案"按钮查看。
- 提示说明：通过游戏界面"提示说明"按钮查看过关思路、螺旋循环策略和当前关卡命令/API 用法。

```python
# 提示：蛇会越来越长，固定路线必须保证蛇头永远不撞到自己的身体
# 思考：怎样走才能覆盖地图上的每一个格子，并且形成循环？

def one_loop():
    # 走完一圈螺旋路径，回到起点
    pass

# 重复走循环，直到收集满 35 颗星星
for loop in range(3):
    one_loop()
```

- 设计说明：这一关是贪吃蛇玩法的完整实现。关键挑战不是"怎么吃到星星"，而是"怎么保证蛇变长后不会撞到自己"。答案是一条覆盖所有格子的闭合循环路径（哈密顿回路）：蛇头始终沿着这条路径走，蛇尾紧随其后，因为路径是闭合的，蛇头永远走在蛇尾前面，不会撞到自己。螺旋路径是最直观的哈密顿回路构造方式。

### 循环路径详解

以 6x6 地图为例，起点 (1,1)（1-based），采用"逐列蛇形 + 顶行回程"的哈密顿回路：

1. 第 1 列向下走到底：(1,1) → (1,2) → (1,3) → (1,4) → (1,5) → (1,6)
2. 向右走一格到第 2 列：(1,6) → (2,6)
3. 第 2 列向上走到第二格：(2,6) → (2,5) → (2,4) → (2,3) → (2,2)（留出 (2,1) 给回程）
4. 向右走一格到第 3 列：(2,2) → (3,2)
5. 第 3 列向下走到底：(3,2) → (3,3) → (3,4) → (3,5) → (3,6)
6. 向右走一格到第 4 列：(3,6) → (4,6)
7. 第 4 列向上走到第二格：(4,6) → (4,5) → (4,4) → (4,3) → (4,2)（留出 (4,1) 给回程）
8. 向右走一格到第 5 列：(4,2) → (5,2)
9. 第 5 列向下走到底：(5,2) → (5,3) → (5,4) → (5,5) → (5,6)
10. 向右走一格到第 6 列：(5,6) → (6,6)
11. 第 6 列向上走到第二格：(6,6) → (6,5) → (6,4) → (6,3) → (6,2)（留出 (6,1) 给回程）
12. 第 1 行向左走回起点：(6,2) → (6,1) → (5,1) → (4,1) → (3,1) → (2,1) → (1,1)

- 路径特点：奇数列向下走到底，偶数列向上走到第二格（留出第 1 行作为回程通道），最后一列走完后沿第 1 行向左回到起点。
- 循环性：走完一圈回到起点 (1,1)，可以无限重复。
- 覆盖性：这条路径覆盖了 6x6 地图的所有 36 个格子（6 + 5×5 + 5 = 36）。
- 自避性：蛇头始终走在蛇尾前面，因为路径是闭合的，蛇头永远不会追上蛇尾。

### 第八关开场故事

- 展示时机：玩家进入第八关后，游戏正式开始前。
- 展示方式：屏幕中央弹窗，背景轻微变暗或模糊。
- 交互方式：玩家点击"开始任务"后关闭弹窗，再进入可操作状态。
- 故事文案：训练场开启了贪吃蛇模式。机器人每吃到一颗星星，身体就会变长一格；地图上会不断出现新的星星，直到机器人占满整个训练场。但有一个规则：机器人不能撞到自己的身体。你需要找到一条能覆盖整个地图、又能循环重复的路径，让机器人头始终走在身体前面，永远不会撞到自己。

### 第八关新增机制

- 蛇身系统：机器人不再只是单个点，而是有长度的蛇身。
  - 蛇身数据结构：记录蛇身各节坐标的队列，队首是蛇头。
  - 移动逻辑：蛇头前进一格，蛇身各节移动到前一节的位置，蛇尾前移（除非吃到星星则不前移）。
  - 渲染：蛇头用不同颜色或样式区分，蛇身用统一颜色。
- 碰撞检测：蛇头新位置如果与蛇身（除蛇尾外）重合，判定失败。
- 星星生成：排除蛇身占据的所有格子后随机选择。

### 第八关待实现任务

- ✅ 扩展引擎支持蛇身系统（蛇身队列、移动、增长）。
- ✅ 实现蛇头撞蛇身的碰撞检测。
- ✅ 实现星星排除蛇身格子的随机生成。
- ✅ 创建第八关关卡文件。
- ✅ 为第八关增加开场故事字段。
- ✅ 为第八关增加提示说明，解释螺旋循环策略。
- ✅ 将第八关接入关卡列表。
- ✅ 提供非完整初始代码和标准答案。
- ✅ 更新 GameCanvas 渲染蛇身。
